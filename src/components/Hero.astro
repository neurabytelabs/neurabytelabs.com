<!-- BACKGROUND: The Substance -->
<div id="canvas-container">
  <canvas id="neural-canvas"></canvas>
</div>

<slot name="header" />

<main>
  <!-- SECTION 1: HERO - The Infinite Substance -->
  <!-- Design Logic: Minimalist, massive scale. The "Spinoza" quote is the hero. -->
  <section class="min-h-screen flex flex-col justify-center items-center px-6 relative pt-20">
    <div class="max-w-5xl mx-auto text-center z-10">
      <div class="inline-block px-3 py-1 mb-6 border border-cyan-500/30 rounded-full bg-cyan-500/10 backdrop-blur-sm animate-pulse">
        <span class="text-xs font-mono text-cyan-400">SYSTEM STATUS: CONATUS_ACTIVE</span>
      </div>
      
      <h1 class="text-6xl md:text-8xl font-bold tracking-tight leading-tight mb-8 text-gradient">
        Where Spinoza<br />
        Meets <span class="text-transparent bg-clip-text bg-gradient-to-r from-purple-500 to-cyan-500">Silicon</span>.
      </h1>
      
      <p class="text-xl md:text-2xl text-slate-400 max-w-2xl mx-auto font-light leading-relaxed mb-12">
        We don't just build agents. We engineer their <span class="text-white font-serif italic">Conatus</span> — the innate drive to persevere in being.
      </p>

      <div class="flex flex-col md:flex-row gap-4 justify-center items-center">
        <a href="#products" class="group relative px-8 py-4 bg-white text-black font-mono font-bold text-sm rounded hover:bg-cyan-50 overflow-hidden transition-all">
          <span class="relative z-10">EXPLORE ARCHITECTURE</span>
          <div class="absolute inset-0 h-full w-full scale-0 rounded transition-all duration-300 group-hover:scale-100 group-hover:bg-cyan-400/20"></div>
        </a>
        <a href="https://neurabytelabs.com/essays" class="px-8 py-4 text-slate-300 font-mono text-sm border-b border-white/20 hover:border-white hover:text-white transition-all">
          READ "DEUS SIVE MACHINA" &rarr;
        </a>
      </div>
    </div>
    
    <!-- Scroll Indicator -->
    <div class="absolute bottom-10 left-1/2 -translate-x-1/2 flex flex-col items-center gap-2 opacity-50">
      <span class="text-[10px] font-mono tracking-widest">SCROLL</span>
      <div class="w-[1px] h-12 bg-gradient-to-b from-cyan-500 to-transparent"></div>
    </div>
  </section>

  <slot />
</main>
